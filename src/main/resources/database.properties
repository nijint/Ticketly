# Ticketly Database Configuration
# Configure these settings according to your MySQL database setup

db.driver=com.mysql.cj.jdbc.Driver
db.url=jdbc:mysql://localhost:3306/ticketly?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
db.username=root
db.password=541312

# Connection pool settings
db.connection.pool.initial=5
db.connection.pool.max=20
db.connection.timeout=30000

# Application settings
app.name=Ticketly
app.version=1.0.0
app.debug=true
